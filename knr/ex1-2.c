#include <stdio.h>

/*
Exercise 1-2. Experiment to find out what happens when prints 's argument string contains
\c, where c is some character not listed above.
*/

int main(void)
{
	printf("this is \c value");
	return 0;
}
